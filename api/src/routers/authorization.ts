import { Router } from "express"
var route = Router()


/*
 * /login
 * Method: post
 * Remarks: Log in a user
 */
route.post('/login', function(response, request) {
	throw "Route yet to be defined"
})


/*
 * /logout
 * Method: post
 * Remarks: Log a user out
 */
route.post('/logout', function(response, request) {
	throw "Route yet to be defined"
})


/*
 * /token
 * Method: post
 * Remarks: Get a login token
 */
route.post('/token', function(response, request) {
	throw "Route yet to be defined"
})


export default route = route

import { Router } from "express"
var route = Router()


/*
 * /
 * Method: post
 * Remarks: Gets all notifications for a user
 */
route.post('/', function(response, request) {
	throw "Route yet to be defined"
})


/*
 * /unread
 * Method: post
 * Remarks: Sets a notification to unread
 */
route.post('/unread', function(response, request) {
	throw "Route yet to be defined"
})


/*
 * /read
 * Method: post
 * Remarks: Sets a notification to read
 */
route.post('/read', function(response, request) {
	throw "Route yet to be defined"
})


export default route = route

const lodash = require("lodash")
const data = require("../data")
const Asset = require("../data/asset")
const AssetTag = require("../data/asset_tag")



/*
 * Class: Assets
 * 	Purpose: Serves as a complex class for the tables asset and asset_tag
 * 	Parameters: name, description, purchase_value, serial_key = undefined, tags = undefined, id = -1
 * 	Functions: 
 */
class Assets {
	constructor(name, description, purchase_value, serial_key = undefined, tags = undefined, id = -1) {
	}
}




module.exports = Assets
